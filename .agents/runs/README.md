# Run evidence policy

F01/F02 contain historical detailed traces from the original hard-delegated SDD. Keep them as history.

For new work, default to one concise file:
`.agents/runs/<feature>/report.md`

For UI_FAST, use at most one file per screen plus an optional final report.

Do not generate one trace file per micro-action unless a strict compliance requirement explicitly needs it.
